#include "qboard.h"

QBoard::QBoard(QWidget *parent/*, Game *game */)
{
//    this->game = game;
//	this->board = new QWidget(parent);
//    this->layout = new QGridLayout();
//    this->board->setLayout(this->layout);
//
//    // pro každý sloupec
//        for (int y = 0; y < 8; y++)
//        {
//            // pro každý řádek
//            for (int x = 0; x < 8; x++)
//            {
////                QField * field = new QField(parent);
////                this->layout->addWidget(field->getField(),x,y);
//				
//            }
//        }
}
